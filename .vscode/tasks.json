{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build KdTools D23", "type": "shell", "command": "msbuild",
            "args": [ "KdTools/KdTools.csproj", "-restore", "-p:Configuration=D23", ],
            "group": { "kind": "build", "isDefault": true }, "presentation": { "reveal": "silent" }, "problemMatcher": "$msCompile"
        },
    ]
}
